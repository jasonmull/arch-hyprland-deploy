# Default monitor layout - used when there is no hosts/<hostname>/ directory.
#
# "preferred" resolution, auto position, auto scale. This works unattended on
# essentially any single-display machine, which is what you want on a box you
# just installed 90 seconds ago.
monitor = , preferred, auto, auto
